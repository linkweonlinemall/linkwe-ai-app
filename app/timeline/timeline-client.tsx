"use client";

import Link from "next/link";
import { Search, SlidersHorizontal, Sparkles, Store } from "lucide-react";
import TimelinePostCard, { type TimelinePost } from "@/components/timeline/TimelinePostCard";

type SearchState = { query: string; type: string; scope: "following" | "all"; photos: boolean };

export default function TimelineClient({ posts, search }: { posts: TimelinePost[]; search: SearchState }) {
  const searching = Boolean(search.query || search.type || search.photos || search.scope === "all");
  return <main className="min-h-screen bg-[radial-gradient(circle_at_50%_0%,rgba(242,138,45,.16),transparent_28%),#F7F5F2] pb-28"><header className="border-b border-white/80 bg-white/75 px-4 py-6 backdrop-blur-xl"><div className="mx-auto max-w-2xl"><div className="flex items-center justify-between gap-4"><div><p className="flex items-center gap-1.5 text-[10px] font-black uppercase tracking-[.2em] text-[#D4450A]"><Sparkles className="size-3.5" /> Shop through posts & ads</p><h1 className="mt-1 text-2xl font-black text-zinc-950 sm:text-3xl">Timeline</h1></div><Link href="/stores" className="shrink-0 rounded-2xl border border-orange-100 bg-white px-4 py-3 text-xs font-bold text-zinc-800 shadow-sm hover:text-[#D4450A]">Find stores</Link></div><form className="mt-5 rounded-[22px] border border-orange-100 bg-white p-3 shadow-[0_12px_35px_rgba(70,43,26,.08)]"><div className="flex gap-2"><label className="relative min-w-0 flex-1"><Search className="absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-zinc-400" /><input name="q" defaultValue={search.query} placeholder="Search products, offers, events or stores…" className="min-h-11 w-full rounded-xl bg-zinc-100 pl-10 pr-3 text-sm outline-none focus:ring-2 focus:ring-orange-200" /></label><button className="flex min-h-11 shrink-0 items-center gap-2 rounded-xl bg-[#D4450A] px-4 text-xs font-black text-white"><Search className="size-4" /><span className="hidden sm:inline">Search</span></button></div><div className="mt-3 flex flex-wrap items-center gap-2"><span className="flex items-center gap-1 text-[10px] font-black uppercase tracking-wider text-zinc-400"><SlidersHorizontal className="size-3.5" /> Filters</span><select name="scope" defaultValue={search.scope} className="min-h-9 rounded-full border border-zinc-200 bg-white px-3 text-xs font-semibold"><option value="following">Stores I follow</option><option value="all">Explore all posts</option></select><select name="type" defaultValue={search.type} className="min-h-9 rounded-full border border-zinc-200 bg-white px-3 text-xs font-semibold"><option value="">Everything</option><option value="PRODUCT">Products</option><option value="SERVICE">Services</option><option value="EVENT">Events</option><option value="STORE">Stores</option><option value="TICKET">Tickets</option></select><label className="inline-flex min-h-9 items-center gap-2 rounded-full border border-zinc-200 px-3 text-xs font-semibold"><input type="checkbox" name="photos" value="1" defaultChecked={search.photos} className="accent-[#D4450A]" /> Photos only</label>{searching ? <Link href="/timeline" className="px-2 text-xs font-bold text-[#D4450A]">Clear</Link> : null}</div></form></div></header>{posts.length ? <div className="mx-auto max-w-2xl space-y-6 px-2.5 py-5 sm:px-4 sm:py-8">{posts.map((post) => <TimelinePostCard key={post.id} initialPost={post} />)}</div> : <div className="mx-auto max-w-xl px-4 py-16 text-center"><span className="mx-auto flex size-20 items-center justify-center rounded-[28px] bg-white text-[#D4450A] shadow-xl shadow-orange-100"><Store /></span><h2 className="mt-6 text-2xl font-black text-zinc-950">{searching ? "No matching posts yet" : "Your timeline is ready"}</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-zinc-500">{searching ? "Try a broader search or explore posts from every store." : "Follow stores you care about and their newest offers, arrivals, events and updates will appear here."}</p><Link href={searching ? "/timeline?scope=all" : "/stores"} className="mt-7 inline-flex min-h-12 items-center rounded-2xl bg-[#D4450A] px-6 text-sm font-black text-white shadow-lg shadow-orange-200">{searching ? "Explore all posts" : "Find stores to follow"}</Link></div>}</main>;
}
