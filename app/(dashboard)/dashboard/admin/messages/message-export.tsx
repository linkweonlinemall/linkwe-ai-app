export default function MessageExport({ conversationId }: { conversationId?: string }) {
  return <details className="my-4 rounded-2xl border border-zinc-200 bg-white p-4"><summary className="cursor-pointer text-sm font-semibold">Export {conversationId ? "this conversation" : "messages"}</summary><form action="/api/admin/messages/export" className="mt-3 flex flex-wrap items-end gap-3">{conversationId && <input type="hidden" name="conversationId" value={conversationId}/>}<label className="text-xs">From (Trinidad time)<input name="from" type="date" className="mt-1 block min-h-11 rounded-xl border px-3"/></label><label className="text-xs">Through<input name="to" type="date" className="mt-1 block min-h-11 rounded-xl border px-3"/></label><label className="flex min-h-11 items-center gap-2 text-xs"><input type="checkbox" name="hideContacts" value="1"/>Exclude names and email addresses from identity columns</label><button className="min-h-11 rounded-xl bg-[#D4450A] px-4 text-sm font-semibold text-white">Download CSV</button><p className="w-full text-xs leading-5 text-zinc-500">Includes message text, sender role, conversation and store IDs, and timestamps. Message text may still contain personal details. Leave dates empty for all messages.</p></form></details>;
}
